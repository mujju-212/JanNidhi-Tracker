Local MongoDB setup

1. Ensure MongoDB is running on your machine.
2. Default connection used by backend:
   mongodb://localhost:27017/jannidhi
3. If you want a different database name or host, update backend/.env.

Optional quick checks:
- Use mongosh to connect and list collections after running the seed script.
